<?php

/**
 * AI 诊断配置
 *
 * 控制 AI 文字诊断（患者端）和 AI 图文诊断（医生端）的行为
 */
return [

    /*
    |--------------------------------------------------------------------------
    | AI 服务模式
    |--------------------------------------------------------------------------
    |
    | mock   - 模拟模式：返回预设的模拟数据（开发阶段使用）
    | remote - 远程模式：调用真实 AI API 接口
    |
    */
    'mode' => env('AI_MODE', 'mock'),

    /*
    |--------------------------------------------------------------------------
    | 千问 VL 图文诊断配置（DashScope）
    |--------------------------------------------------------------------------
    */
    'qwen' => [
        'url'     => env('QWEN_API_URL', 'https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions'),
        'key'     => env('QWEN_API_KEY', ''),
        'model'   => env('QWEN_MODEL', 'qwen3-vl-plus'),
        'timeout' => env('QWEN_TIMEOUT', 60),
    ],

    /*
    |--------------------------------------------------------------------------
    | 模拟数据配置
    |--------------------------------------------------------------------------
    */
    'mock' => [
        // 文字诊断模拟延迟（秒），模拟真实 API 响应时间
        'text_diagnosis_delay' => env('AI_MOCK_TEXT_DELAY', 2),

        // 图文诊断模拟延迟（秒）
        'image_diagnosis_delay' => env('AI_MOCK_IMAGE_DELAY', 3),
    ],
];
