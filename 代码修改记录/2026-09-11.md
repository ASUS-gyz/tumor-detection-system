# 代码修改记录 2026-09-11

## 变更 1：初始化 Agent skills 配置（docs/agents/ + CLAUDE.md）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 11:43:26

### 删除范围

无删除。

### 新增范围

1. `docs/agents/issue-tracker.md`（新增）：issue 跟踪约定 —— GitHub Issues（ASUS-gyz/tumor-detection-system），gh CLI 操作命令、"PRs as a request surface" 标志默认 no、wayfinder 的 map/child-ticket/blocking 操作约定。
2. `docs/agents/triage-labels.md`（新增）：五个标准 triage 角色与标签串的默认映射表（needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix）。
3. `docs/agents/domain.md`（新增）：领域文档消费规则 —— 单上下文布局（根目录 CONTEXT.md + docs/adr/），缺失时静默跳过由 /domain-modeling 惰性创建，输出术语遵循词汇表，与 ADR 冲突须显式指出。
4. `CLAUDE.md`（修改）：文件末尾追加 `## Agent skills` 段（Issue tracker / Triage labels / Domain docs 三个小节，指向上述三个文件）。

### 说明

由 `/setup-matt-pocock-skills` 生成；供 to-tickets、triage、to-spec、domain-modeling、wayfinder 等工程技能读取。后续可直接编辑 `docs/agents/*.md` 调整，仅在切换 issue 跟踪器或重置配置时需重新运行该 skill。

## 变更 2：修复库存双轨制分叉（全项目 bug 审查 · 高危 #1）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 14:03:14

### 删除范围

1. `app/Http/Services/GYZ/AdminStatisticsService.php` `drugConsumption()`：删除对 `drug_stock_changes` 表（ZZT 取药出库台账）的查询及与 `stock_movements`（GYZ 台账）的合并循环（原 49-89 行的双表读取 + foreach 合并逻辑）。

### 新增范围

1. `app/Http/Controllers/Api/ZZT/PrescriptionController.php` `confirm()`（取药出库事务）：
   - 出库时同步扣减 `drugs.stock_quantity`（此前只扣 `drug_stocks.quantity`，导致 GYZ 侧全部库存视图虚高、低库存预警永不触发）；
   - 加锁顺序调整为先锁 `drugs` 再锁 `drug_stocks`，与 GYZ 入库（`DrugService::stockIn`）一致，避免死锁；
   - 库存校验改为两侧取 min 复查（任一侧不足即回滚）；
   - 新增写入 `stock_movements` 出库流水（type=out，reference_type=prescription_dispense），此前管理端出库统计/流水/药品消耗恒为 0；
   - 新增 `use App\Models\StockMovement;` 导入。
2. `app/Http/Services/GYZ/DrugService.php` `create()`：新增药品后同步建立 `drug_stocks` 行（quantity 与初始库存一致），此前新药在发药端被视为零库存、永远"库存不足"。
3. `app/Http/Services/GYZ/AdminStatisticsService.php` `drugConsumption()`：改为只读统一台账 `stock_movements`（处方发药出库现已写入该表，避免同一批发药在两表合并时双计）。

### 说明

- 属全项目 bug 审查（2026-09-11）高危 #1「库存双轨制分叉」：发药只扣 `drug_stocks`、入库以 `drugs` 覆写 `drug_stocks`、新药不建 `drug_stocks`、`stock_movements` 无 out 写入方，四个缺口一并修复。修复后双轨两侧由所有写入方保持一致，`stockIn` 的覆写语义不再产生"复活"。
- 不涉及数据库结构修改。
- 待办：RDS 连接中断（TCP 3306 超时，实例疑似欠费停机或白名单变更），端到端取药验证待数据库恢复后补做。

## 变更 3：修复禁用账号 token 失效与排班约束失效（全项目 bug 审查 · 高危 #2 #3）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 14:18:15

### 删除范围

1. `app/Http/Controllers/Api/ZZT/PatientController.php` `store()`：删除原事务外的患者进行中预约检查与无约束直接建单逻辑。
2. `app/Http/Controllers/Api/ZZT/PatientController.php` `availableSlots()`：删除硬编码 8 时段、仅按 pending/called/in_progress 判断占用的旧逻辑。

### 新增范围

1. `app/Auth/SanctumGuard.php`：token 认证通过后检查 `users.status`，已禁用账号立即返回未认证并吊销其全部存量 token（此前禁用用户持有旧 token 可无限期调用全部接口）。
2. `app/Auth/HasApiTokens.php` `createToken()`：token 创建时写入 `expires_at = now()+30天`（此前永不过期）。
3. `app/Http/Services/GYZ/AdminUserService.php` `toggleStatus()`：管理员禁用用户时同步吊销其全部 API token（对齐 ZZT 注销账号行为，与 Guard 侧检查双保险）。
4. `app/Http/Controllers/Api/ZZT/PatientController.php`：
   - `store()` 重构为事务内创建：保留患者进行中预约检查，新增 `assertSlotBookable()` 排班约束（停诊拒绝 / 时段须在排班 `time_slots` 内 / 单时段占用复查（pending/called/in_progress/completed 均算占用）/ 当日 `max_patients` 上限）。以该医生该星期几的排班行为锁载体，无排班行时 `upsert` 原子补建默认行（可约、默认时段、20/日），保证并发预约串行；
   - 新增私有方法 `assertSlotBookable()`；
   - `availableSlots()` 与同一套约束对齐：停诊日返回空、时段列表优先取排班 `time_slots`（为空回落默认 8 时段）、completed 也算占用、当日号源满则 `available_slots` 清空。响应字段（date/all_slots/booked_slots/available_slots）不变；
   - 新增 `use App\Models\DoctorSchedule;`、`use Illuminate\Support\Facades\DB;` 导入。

### 说明

- 高危 #2「禁用账号的存量 token 永久有效」：登录处原有 status 检查仅拦截新登录，存量 token 不受影响；本次从 Guard、签发、禁用动作三处闭环。
- 高危 #3「排班模块与预约流程脱节」：`doctor_schedules` 的 is_available/time_slots/max_patients 自迁移后无任何读方参与预约创建；无 (doctor_id, date, time) 唯一约束（不改表结构的前提下）由事务 + 排班行锁 + 占用复查实现同语义防护。不涉及数据库结构修改。
- 待办：RDS 恢复后统一补做 #1 取药双表同步、#2 禁用后旧 token 401、#3 重复预约被拒三组端到端验证。

## 变更 4：修复 BulkDataSeeder 双重 JSON 编码（验证 #3 时发现的数据质量缺陷）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 15:04:17

### 删除范围

1. `database/seeders/BulkDataSeeder.php` `seedDoctorSchedules()`：删除 `'time_slots' => json_encode($this->timeSlots)` 的预编码写法（模型 array cast 会再编码一次，导致入库值双重编码，cast 后得到字符串而非数组，排班时段校验失效）。
2. `database/seeders/BulkDataSeeder.php` `seedAiDiagnoses()`：删除 `'possible_conditions' => json_encode([...])` 的预编码写法（同因）。

### 新增范围

1. `database/seeders/BulkDataSeeder.php`：`'time_slots' => $this->timeSlots`、`'possible_conditions' => [...]`，均传 PHP 数组交由模型 array cast 统一编码，并加注释说明勿预 json_encode。

### 说明

- 验证变更 3 的排班校验时发现：bulk 系医生（doctor 5-10）共 42/49 行 `doctor_schedules.time_slots`、15/18 行 `ai_diagnoses.possible_conditions` 为双重编码（如 `"[\"08:30\",...]"`），模型 cast 后是字符串不是数组，时段校验对这些医生不生效。数据修复已记录至数据库变更记录。修复后 bulk 医生预约路径实测：排班正常生效、非法时段 10001 拒绝。
- GYZTestDataSeeder 写法正确（传数组），doctor_li（doctor 2）数据未受影响。

## 变更 5：变更 2/3/4 端到端验证通过（RDS 恢复后补做）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 15:04:17

### 验证结果（全部通过，无代码改动）

1. **#1 库存双轨**：处方 13（bulk_patient1，药品 17×2）取药 → `drugs.stock_quantity` 与 `drug_stocks.quantity` 双表 39→37 同步扣减；`stock_movements` 新增 out 流水（before 39 / after 37 / reference prescription_dispense#13）；`drug_stock_changes` 台账同步写入。
2. **#2 禁用失效**：admin 禁用 user 12 → 其旧 token 立即 401（20001 未登录）、token 表 0 条、禁用期重登录被拒（20006 账号已被禁用）；恢复启用后正常。新签发 token `expires_at=+30天`（存量 NULL 的旧 token 受 Guard 状态检查保护）。
3. **#3 排班约束**：周日（dow=0 停诊）预约被拒 40002「该医生在所选日期停诊」；正常时段预约成功；同患者重复预约被拦（40009 进行中检查）；跨患者抢占同一时段被拦（日志 trace 2f33700f，40009「所选时段已被预约」）；`available_slots` 随预约实时更新、响应结构不变；当日 `max_patients`、排班 `time_slots` 校验随数据修复后生效。

## 变更 6：打通通知与操作日志写入链路（GitHub issue #21）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 15:25:18

### 删除范围

1. `app/Http/Services/GYZ/NotificationService.php`：删除无容错的 `Notification::create` 直接写入（通知写库失败会导致主业务 500）。
2. `app/Http/Services/GYZ/OperationLogService.php`：删除固定取 `auth()->user()` 为操作人的写入逻辑（登录场景下 auth 未就绪，操作人会误记为"系统"）。

### 新增范围

1. `app/Http/Services/GYZ/NotificationService.php`：`send()` 改为尽力而为——try/catch 包裹写入，失败仅记错误日志，不阻断主业务。
2. `app/Http/Services/GYZ/OperationLogService.php`：`log()` 新增可选参数 `$operatorId/$operatorName`，供登录等 auth 未就绪场景显式传操作人。
3. `app/Http/Services/GYZ/DoctorAppointmentService.php`：`call()` 成功后通知患者（type=appointment_call）；`reject()` 成功后通知患者（type=system，预约被拒）。
4. `app/Http/Controllers/Api/ZZT/PrescriptionController.php`：
   - `store()` 成功后通知患者（type=prescription_ready）；
   - `confirm()` 提交后检查各药品库存，低于 `DrugService::LOW_STOCK_THRESHOLD` 的通知全部 active 管理员（type=stock_warning，旁路 try/catch 不阻断取药）；
   - 新增 `User`、`DrugService`、`NotificationService`、`Log` 导入。
5. `app/Http/Services/GYZ/AdminUserService.php`：`create()`/`update()`/`toggleStatus()` 分别写操作日志（action=create/update/status_change，module=user），禁用时备注已同步吊销 token。
6. `app/Http/Controllers/Api/GYZ/AdminUserController.php`：`batchImport()` 成功后写摘要操作日志；新增 `OperationLogService` 导入。
7. `app/Http/Controllers/Api/ZZT/AuthController.php`：登录成功写 action=login（显式传操作人），登出写 action=logout（module=system）。

### 说明

- 通知类型与操作日志动作均取自迁移注释声明的词表（notifications.type：appointment_call/stock_warning/prescription_ready/system；operation_logs.action：create/update/delete/login/logout/status_change）。
- 不涉及数据库结构修改（两表已存在，仅补写入方）。
- 端到端验证：患者预约→医生叫号→开处方，患者通知列表依次收到 appointment_call（appointment#43）、prescription_ready（prescription#35）；stock_warning（drug#2）由发药触发；operation_logs 落库 login/logout/create/status_change 五条，操作人与目标正确。

## 变更 7：批量接口补校验并支持部分成功清单（GitHub issue #22）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 15:29:13

### 删除范围

1. `app/Http/Controllers/Api/GYZ/DrugController.php` `batchStockIn()`：删除无校验直接读 items、逐项调 service 且失败即整批 500、无成功清单的旧实现。
2. `app/Http/Controllers/Api/GYZ/AdminUserController.php` `batchImport()`：删除无校验原始透传 `` 给 service、单条失败中断整批、无清单的旧实现（含原 users 空数组时的手写 PARAM_ERROR 分支）。

### 新增范围

1. `app/Http/Controllers/Api/GYZ/DrugController.php` `batchStockIn()`：
   - items 走与单条一致的校验（`items.*.drug_id` required|integer|exists、`items.*.quantity` required|integer|min:1、`items.*.remark` max:255，数组上限 200）；
   - 逐条 try/catch 隔离（BusinessException 取业务错误信息、\Throwable 记日志返回"系统异常"），响应含 total/success/failed/results 逐条清单。
2. `app/Http/Controllers/Api/GYZ/AdminUserController.php` `batchImport()`：
   - users 数组校验（required|min:1|max:200）；
   - 逐条复用 `AdminUserCreateRequest::rules()` 经 `Validator::make` 校验，未过则记该行错误；
   - service create 逐条隔离（BusinessException/Throwable 均记入该行，不中断整批）；
   - 响应含 total/success/failed/results（含 user_id 或逐条 error）；操作日志改为摘要"成功 X 个，失败 Y 个"。
3. 两文件分别新增 `BusinessException`、`Log`、`Validator` 导入。

### 说明

- 修复缺陷：批量入库此前可传 `quantity: -5` 直改负库存、缺 key 直接 500；批量导入此前缺 password 触发 500、批内重复邮箱中断整批且无成功清单。
- 不涉及数据库结构修改。
- 端到端验证：批量入库 合法 2/2 成功、负数与缺 quantity 均 10001 拒绝；批量导入 4 行混合 → 成功 1（user 45）、坏邮箱/重复邮箱/缺密码各行给出对应错误，互不影响。

## 变更 8：AI 诊断 appointment_id 补 exists 与归属校验（GitHub issue #23）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 07:42:50

### 删除范围

1. `app/Http/Services/GYZ/DoctorAiDiagnosisService.php` `create()`：删除无角色限制的 `User::find($patientId)` 患者查询（此前医生可将诊断挂到任意用户名下，包括其他医生）。

### 新增范围

1. `app/Http/Controllers/Api/ZZT/AIDiagnosisController.php` `store()`（患者文字诊断）：
   - `appointment_id` 校验升级为 `nullable|integer|exists:appointments,id`（此前仅 integer，可传不存在的 id）；
   - 新增归属检查：传入 `appointment_id` 时须为当前患者本人的预约，否则 30001「预约不存在」；
   - 新增 `use App\Models\Appointment;` 导入。
2. `app/Http/Services/GYZ/DoctorAiDiagnosisService.php` `create()`（医生图文诊断）：
   - 患者查询追加 `where('role', 'patient')`，非患者用户（医生/管理员）返回「患者不存在」；
   - 新增预约归属检查（在图片落盘前执行）：`appointment_id` 非空时预约必须存在、`doctor_id` 等于当前医生、`patient_id` 等于所诊断患者，否则 30001「预约不存在或不属于该患者」——此前医生可将自己的诊断挂到其他医生的预约上；
   - 新增 `use App\Models\Appointment;` 导入。

### 说明

- 修复缺陷：患者/医生均可将 AI 诊断记录挂到任意（含他人或不存在）的 appointment_id 上，造成病历归属错乱。
- 不涉及数据库结构修改。
- 端到端验证（8 项全过）：患者侧 他人预约 404 拒绝、不存在 id 422 拒绝、本人预约 200 成功、不传 id 200 成功；医生侧 跨医生预约 404 拒绝、预约与患者不匹配 404 拒绝、patient_id 传医生角色 404「患者不存在」、本人预约+匹配患者 200 成功（诊断 #39 正确落库 appointment_id=43）。

## 变更 9：医学影像迁入私有磁盘，签名 URL 鉴权访问（GitHub issue #24）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 07:55:25

### 删除范围

1. `app/Http/Services/GYZ/DoctorAiDiagnosisService.php` `create()`：删除 `$image->store('ai-images', 'public')` 公有磁盘存储与 `Storage::url()` 生成永久公开地址的逻辑。
2. `app/Http/Services/GYZ/DoctorAiDiagnosisService.php`：删除 `use Illuminate\Support\Facades\Storage;` 导入。
3. `app/Http/Middleware/ApiLogMiddleware.php`：删除 `$response->status()` 取状态码写法（StreamedResponse 无此方法，流式响应直接 500）。

### 新增范围

1. `app/Http/Services/GYZ/DoctorAiDiagnosisService.php` `create()`：影像改存 `local` 私有磁盘（storage/app/private，无公网入口）；`ai_diagnoses.image_url` 列改为只存文件名；响应中 `image_url` 改为 30 分钟有效的限时签名 URL。
2. `app/Http/Services/GYZ/DoctorAiDiagnosisService.php` 新增私有方法 `imageUrl()`：由文件名实时生成签名 URL（`URL::temporarySignedRoute`），`basename()` 归一化兼容历史 `/storage/` 格式数据；`create()` 与 `detail()` 输出统一走该方法。
3. `app/Http/Controllers/Api/GYZ/DoctorAiDiagnosisController.php` 新增 `serveImage(string $file)`：私有磁盘取流返回（`Storage::disk('local')->response()`），文件名白名单正则 `^[A-Za-z0-9._-]+$` 防目录穿越，文件缺失 404。
4. `routes/api.php` 新增免登录影像路由 `GET /api/ai-image/{file}`（name=ai-image.show，middleware=signed，单段文件名正则约束）。
5. `bootstrap/app.php` 新增 `InvalidSignatureException` 渲染：返回 20005「链接已失效或签名不正确」（HTTP 403）——此前会落入兜底渲染成 90001 系统异常。
6. `app/Http/Middleware/ApiLogMiddleware.php`：状态码改用 `$response->getStatusCode()`（Symfony Response 基类方法，各类响应通用）。
7. 新增 `Storage`、`StreamedResponse` 导入。

### 说明

- 修复缺陷：医学影像（CT/MRI 等敏感数据）存公有磁盘，`/storage/ai-images/xxx.jpg` 无任何鉴权即可访问（本地未建 storage:link 故未暴露，但任何部署执行该命令即全量公开）。
- 数据修复：10 个影像文件由 `storage/app/public/ai-images/` 迁至 `storage/app/private/ai-images/`（公有目录清空），3 行存量 `image_url` 由 `/storage/ai-images/xxx` 重写为文件名，已记录数据库变更记录。
- 前端无感知：`image_url` 仍是可直接放入 `<img src>` 的完整 URL，仅变为带过期时间的签名链接，30 分钟后自动失效，重新拉详情即刷新。
- 端到端验证（10 项全过）：新上传返回签名 URL；无登录+有效签名 200 取图（image/jpeg）；篡改签名/去签名/假签名 403（20005）；路径穿越 404；有效签名+不存在文件 404；旧公开路径不再可访问；历史记录（修复后）详情签名 URL 正常取图（108KB jpeg）。
- 顺带修复：ApiLogMiddleware 对流式响应的 500（status() 方法不存在）。

## 变更 10：取药确认事务内行锁复查状态，消除并发重复出库（GitHub issue #26）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 08:20:03

### 删除范围

无删除（仅插入与改写 catch 分支）。

### 新增范围

1. `app/Http/Controllers/Api/ZZT/PrescriptionController.php` `confirm()`：
   - 事务内新增 `Prescription::with('items')->where('id', $rx->id)->lockForUpdate()->first()` 行锁复查：拿到锁后状态已非 pending 即抛 40002（dispensed 提示「已取药」，其余「状态不可操作」）。此前状态检查在事务外，两个并发请求可同时通过检查后先后扣减库存，造成双重出库；
   - catch 拆分：BusinessException 原样重抛（保留准确的业务错误码），其余异常仍统一包装为「取药失败」40001。
2. 锁顺序：prescriptions 行锁 → drugs → drug_stocks，与事务内既有加锁顺序及 GYZ 入库（仅锁后两者）无交叉，无死锁环。

### 说明

- 端到端验证（并发实测）：处方 #36 pending，两路并发 confirm → A 200「取药成功」、B 40002「已取药」；drug2 双侧库存 55→54 仅扣一次，stock_movements 与 drug_stock_changes 各恰好 1 条 out 流水（qty=1 before=55 after=54），处方状态 dispensed。
- 不涉及数据库结构修改。

## 变更 11：中危缺陷批次修复 11 项（GitHub issue #27）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 16:56:25

### 逐项清单（删除范围 → 新增范围）

1. **模型类名大小写**：`AIDiagnosisController.php`（8 处 import/调用/类型提示）与 `PatientController.php`（1 处）对模型的 `AIDiagnosis` 错误拼写统一改为与文件 `AiDiagnosis.php` 一致的 `AiDiagnosis`（控制器/服务自身文件名与类名本就一致，无需动）。此前 Linux 部署全部患者端 AI 接口 500。
2. **时区**：`config/app.php` `'timezone'` UTC → `Asia/Shanghai`。此前北京 0-8 点数据落前一天。
3. **内联校验接线死代码 FormRequest**：`CreatePrescriptionRequest`（dosage max:255 等）接入 `PrescriptionController::store()`；`CreateMedicalRecordRequest`/`UpdateMedicalRecordRequest` 接入 `MedicalRecordController::store()/update()`；`CreateDiagnosisRequest` 接入 `AIDiagnosisController::store()`；4 个 Request 由死代码转为在用，超长输入 422 而非撞库 60001。
4. **AI 服务异常可解释**：`QwenVisionService` 三处 `\RuntimeException` 改 `BusinessException(50001)`，HTTP 调用包 `ConnectionException` → 50001「AI 诊断服务连接失败」。
5. **事务异常落日志**：`PrescriptionController` 取药/开处方两处 catch 补 `Log::error`（含处方号、原始异常与文件行号）。
6. **角色变更审计**：`AdminUserService::update()` 角色变更记录新旧值（"X → Y"）至操作日志，写 business 警告日志，并吊销目标全部 token 强制重新登录（role 值白名单 in:patient,doctor,admin 原已有）。
7. **排班部分更新语义**：`DoctorScheduleService::set()` 由 `updateOrCreate` 全字段覆盖改为 firstOrNew + 仅应用显式提交字段（缺省值仅用于新建行），避免部分更新静默恢复停诊/默认时段；并发首设撞唯一键 1062 时取既有行按更新路径重放一次，不再 500。
8. **取消预约守卫**：`MedicalRecordController::store()` 与 `PrescriptionController::store()` 增加 `status === 'cancelled'` → 40002 拒绝。
9. **死配置与 mock 阻塞**：删除 `config/ai.php` 零引用的 DeepSeek `ai.api.*` 块与 `ai.mock.*` 延迟配置；删除 `AIDiagnosisService` 两处 `sleep()`（mock 结果为预设数据，不再阻塞 2-3 秒）。
10. **状态流转加锁**：`DoctorAppointmentService` 新增 `transition()`（事务 + 行锁 + 锁后复查），call/start/reject 改走该助手；`complete()` 病历/处方存在性检查移入行锁内；删除失去调用方的 `findOwn()`；患者 `PatientController::cancel()` 同步改为事务+行锁。叫号/接诊/拒诊与取消不再互相覆盖。
11. **并发唯一键冲突业务化**：`DrugService::create()/update()`（同名药品）与 `AdminUserService::create()/update()`（同邮箱）在落库处捕获 1062 → 30003 业务错误码，不再 500。

### 说明

- 端到端验证（A-H 组全过）：dosage 300 字符 422；病历创建 200/重复 40009；取消预约写病历/开处方均 40002 且文案明确；排班部分更新只动提交字段（max=5 与 is_available=false 互不干扰）后已恢复默认；user45 提为 admin 审计日志含新旧值且 token 清零（已恢复 doctor）；重复叫号 40002、call→start→complete 链路（行锁路径）实测走通至 completed；重复邮箱 30003；AI 文字诊断在 config 调整后 200。
- 注意事项：时区切换前入库的历史行仍为 UTC 墙上时间（偏早 8 小时），属测试数据；如需精确可重跑 BulkDataSeeder。不涉及数据库结构修改。

## 变更 12：低危缺陷批次修复 9 项（GitHub issue #28）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 17:22:58

### 删除范围

1. `app/Exceptions/Handler.php` 整文件删除（Laravel 11 起异常渲染已迁移至 `bootstrap/app.php`，全仓零引用的死代码）。
2. `AdminDashboardService::applySort()` 删除 `[$col, $dir] = explode(':', $sort)` 双变量解构写法（sort 无冒号时 Undefined key 报错）。
3. `DrugService::LOW_STOCK_THRESHOLD` 常量在业务代码中的 6 处直接引用（DrugService list/is_low_stock、PrescriptionController、AdminDashboardService、AdminStatisticsService、DoctorAppointmentService）全部移除（常量本身保留作兜底默认值）。
4. `QwenVisionService::parseResponse()` 的 `array $body` 强制非空签名删除。
5. `DoctorAiDiagnosisService::create()` 中「先落盘影像、后调 AI」的顺序删除。
6. `SystemConfigService::update()` 对任意新 key 直接 `SystemConfig::set()`（updateOrCreate 静默建行）的行为移除。
7. `NotificationService::markRead()` 无返回值的 void 签名移除；`NotificationController::markRead()` 恒返成功的逻辑移除。

### 新增范围

1. **风险词归一化**（issue #28 项 1）：`AIDiagnosisService` 新增 `normalizeRiskLevel()` 静态助手（高/中/低 关键字 → 高风险/中风险/低风险，无匹配归「未知」）；`callRemoteAI()` 对 `risk_level`/`risk_assessment` 两字段做归一化；mock 图文诊断 `'中度风险'` → `'中风险'`；`QwenVisionService::parseResponse()` 的 `risk_assessment` 输出同样归一化。全链路风险词统一为三级标准词。
2. **低库存阈值可配置化**（项 2）：`DrugService` 新增 `lowStockThreshold(): int`（读 `system_configs.stock_low_threshold`，缺失/非法回退常量 10），上述 6 处调用点全部切换，阈值改配置即时生效。
3. **排序参数容错**（项 3）：`AdminDashboardService::applySort()` 改为 `explode` 后取 `$parts[0]`/`$parts[1] ?? 'desc'`，`sort=id`（无方向）合法，白名单校验保持不变。
4. **月度趋势口径修正**（项 5）：`AdminStatisticsService::monthlyTrend()` 预约计数由 `created_at` 改按 `appointment_date` 归桶（预约可提前任意天数创建）；其余四项保持 `created_at`。
5. **工作量完成率口径修正**（项 5）：`doctorWorkload()` 增加 `cancelled` 聚合列并新增返回字段 `cancelled_appointments`，`completion_rate` 分母剔除已取消（completed/(total-cancelled)，分母为 0 时返回 0）。
6. **通知列表布尔过滤**（项 6）：`NotificationService::list()` 的 `is_read` 过滤值先经 `filter_var(..., FILTER_VALIDATE_BOOLEAN)` 归一化，`is_read=true/false/1/0` 字符串均正确过滤。
7. **标记已读可感知**（项 9）：`markRead()` 返回受影响行数；控制器为 0 时抛 `BusinessException('通知不存在', 30001)`。
8. **配置项白名单**（项 7）：`SystemConfigService::update()` 先校验全部 key 均已存在于 `system_configs` 表，存在未知 key 整批拒绝（10001「配置项不存在：xxx」），不再静默新建任意配置；合法更新值统一强转字符串。
9. **排班时段格式校验**（项 7）：`DoctorScheduleRequest` 的 `time_slots.*` 增加 `date_format:H:i` 规则及中文文案，`09:00` 合法、`abc`/`9点` 422。
10. **AI 响应空体防护**（项 8）：`QwenVisionService::parseResponse()` 签名放宽为 `?array`，`$resp->json()` 返回 null 时按「AI 返回内容为空」50001 处理，不再 TypeError；`$text` 增加 `(string)` 强转与 `=== ''` 判断。
11. **AI 失败不残留孤儿影像**（项 8）：`DoctorAiDiagnosisService::create()` 调整为「先调 AI 分析、后 `store()` 落盘」，AI 失败抛错时不产生无主文件。

### 说明

- 端到端验证（全过）：`PUT /notifications/999999/read` → 30001「通知不存在」；`sort=id` 无冒号 200 且按 id 降序；`is_read=true/false` 过滤实测 4 条未读/0 条已读区分正确；配置更新含未知 key → 10001 且库里无残留、合法更新 `stock_low_threshold=15` 后管理端 dashboard `low_stock_drugs` 立即变化（已恢复 10）；排班 `time_slots:["09:00","abc"]` → 10001「时间段格式必须为 HH:mm」、合法时段 200（D5 周一排班已恢复默认）；monthly-trend 3 个月输出合理；doctor-workload dr2=6/(12-4)=75%、dr5=100% 与剔除取消后的口径一致且含 `cancelled_appointments` 字段；`normalizeRiskLevel` 对 中度风险/高/低/空串 等 8 组用例映射正确（恶性/良性等无关键字词按约定归「未知」）；AI 文字诊断 mock 正常（测试行已清理）。
- 不涉及数据库结构修改；`drug_stocks.min_stock` 字段维持未消费现状（结构变更需另行授权）。

## 变更 13：评价/续方假成功修复（GitHub issue #25，含已授权数据库变更）

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-11 17:37:18

### 数据库变更（用户已明确授权）

- 新建 `reviews` 表，迁移文件 `database/migrations/2026_09_11_172957_create_reviews_table.php`，已于 2026-09-11 17:30:21 在 RDS 执行成功。
- 完整 SQL 与设计说明见 `数据库变更记录/reviews.md`（变更 1：表添加）。要点：appointment_id 唯一约束（一预约一评价）、patient_id/doctor_id 外键、doctor_id 索引、rating tinyint 1-5、content varchar(500) 可空。
- 未修改任何现有表结构；续方复用现有 `prescriptions` 表（pending 状态新处方），无枚举变更。

### 删除范围

1. `PatientController::review()` 原样回显输入即返回成功的假实现。
2. `PrescriptionController::refill()` 仅查询并回显原处方条目、不落库不建单不通知的单行假实现。

### 新增范围

1. **Review 模型**：`app/Models/Review.php`（fillable + appointment/patient/doctor 关联）。
2. **评价真实落库**（`PatientController::review`）：预约归属与 completed 状态校验保持；新增重复评价预检查（40009「该就诊已评价，请勿重复提交」）；`Review::create` 落库并对并发双击捕获 1062 唯一键 → 40009 兜底；返回评价 id/评分/内容/时间。
3. **续方真实建单**（`PrescriptionController::refill` 重写为事务）：行锁锁定患者原处方（status 非 dispensed → 40002）；同预约下已存在未取药处方 → 40009 防重复续方；按原处方复制新建 `pending` 状态处方及全部 `prescription_items` 条目（同预约同医生）；`NotificationService` 通知医生（prescription_ready，含原/新处方号）；写 business 日志；返回新处方 id + 复制条目。

### 说明

- 端到端验证（全过）：评价 appt43（rating=4）→ 200 返回评价 id=1 且库中落行；重复评价 → 40009；rating=6 → 422（10001）。续方 rx36 → 200 新建 rx37（pending，条目 奥希替尼片/1 tab tid/qty1 原样复制），重复续方 → 40009；对未取药处方续方 → 40002；医生端（user 5）pending 列表首位即 rx37 且收到「续方申请」通知；rx37 已走 confirm 取药完成闭环（库存正常扣减）。
- 测试遗留：reviews 表保留 1 条评价测试行（appt43，一预约一评价约束下无副作用）；rx35/rx37 已取药闭环，无悬挂 pending。
- 不涉及数据库结构修改之外的其它变更（本变更 13 的建表为唯一结构变更，已按规范记录）。
