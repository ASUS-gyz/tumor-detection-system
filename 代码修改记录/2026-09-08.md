# 代码修改记录 2026-09-08

## 变更 1：.env 数据库连接配置切换至新 RDS

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-08 11:24:46
- **涉及文件**：`.env`

### 删除范围

`.env` 中数据库连接配置（第 24、26、27、28 行）：

```env
DB_HOST=rm-cn-xot4w0n3h0001ico.rwlb.cn-chengdu.rds.aliyuncs.com
DB_DATABASE=tumor_system
DB_USERNAME=tumor_user
DB_PASSWORD=Zzt070602
```

### 新增范围

替换为新的阿里云 RDS 连接配置：

```env
DB_HOST=rm-bp1362om983d343hdpo.mysql.rds.aliyuncs.com
DB_DATABASE=tumor-detection-system
DB_USERNAME=nick3814674495
DB_PASSWORD=Gyz032411!
```

### 说明

- 原 RDS 实例 DNS 失效无法连接，经用户确认切换至新实例并授权新建数据库 `tumor-detection-system`。
- 已执行 `php artisan config:clear` 清除配置缓存。
- 已通过 `php artisan db:show` 验证连接成功（MySQL 8.4.7）。
- 旧实例数据因实例释放无法导出，未能迁移（回收站亦为空）。

## 变更 2：修复迁移顺序——重命名两个药品库存迁移文件

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-08 11:33:00
- **涉及文件**：`database/migrations/`

### 删除范围

- `database/migrations/2024_01_01_000009_create_drug_stocks_table.php`
- `database/migrations/2024_01_01_000010_create_drug_stock_changes_table.php`

### 新增范围（同内容文件重命名，仅文件名变更）

- `database/migrations/2026_07_27_142110_create_drug_stocks_table.php`
- `database/migrations/2026_07_27_142120_create_drug_stock_changes_table.php`

### 说明

- 两个迁移引用 `drugs` 表外键，但原文件名（2024_01_01_*）使其排在 `2026_07_27_142100_create_drugs_table.php` 之前执行，导致 `SQLSTATE[HY000]: 1824 Failed to open the referenced table 'drugs'`。
- 重命名后执行顺序调整为：drugs(142100) → drug_stocks(142110) → drug_stock_changes(142120) → stock_movements(142200)。
- 迁移文件内容未做任何修改。

## 变更 3：批量测试数据 Seeder + 修复 AI 诊断两处 bug

- **修改人**：ASUS-gyz（git config user.name）
- **修改时间**：2026-09-08 12:30:00

### 新增代码记录

1. `database/seeders/BulkDataSeeder.php`（新增）：把每张业务表补足至 30 条以上的幂等 Seeder（users 30、doctor_schedules 49、drugs/drug_stocks 33、appointments 35（completed 30）、medical_records 30、prescriptions 30、prescription_items 45、ai_diagnoses 30、drug_stock_changes 30、stock_movements 30、notifications 30、operation_logs 30、system_configs 30）。框架运行时表（cache/jobs/sessions/personal_access_tokens/password_reset_tokens）不做假数据填充。
2. `database/seeders/DatabaseSeeder.php`：Seeder 调用改为 `[GYZTestDataSeeder::class, BulkDataSeeder::class]`。

### 修改代码记录（bug 修复）

1. `app/Http/Services/ZZT/AIDiagnosisService.php` `callRemoteAI()`：
   - 配置键修复：`config('ai.api.*')` → `config('ai.qwen.*')`（原键在 config/ai.php 中不存在，取值 null 导致远程调用必然失败，接口报 502/50001）。
   - 健壮性修复：远程模型返回的 JSON 中非列表字段若为数组（如 analysis），拍平为字符串后再入库，避免 `Array to string conversion`（60001 数据库异常）。
2. `app/Http/Controllers/Api/GYZ/DoctorAiDiagnosisController.php` `store()`：`$request->integer('appointment_id')` 在未传时返回 0，触发 `ai_diagnoses.appointment_id` 外键违规（60001）；改为未传时传 null（服务签名本为 `?int`）。

### 删除代码记录

无（仅上述修改与新增，无删除）。

### 验证

88 个业务接口（route:list 全量）用 bash+curl 脚本按业务正确顺序实测：全部返回 code=0 成功，含患者 AI 文字诊断（远程千问）、医生 AI 图文诊断（真实 CT 图片）、预约→叫号→接诊→病历→处方→结束全流程。
