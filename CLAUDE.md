1.数据库修改规范

1.1默认禁止修改数据库

在任何开发任务中：不允许主动修改数据库结构；不允许新增字段；不允许删除字段；不允许修改字段类型；不允许修改字段长度；不允许删除表；不允许修改索引；不允许修改约束。如业务确实需要修改数据库，必须先获得明确授权。

1.2 数据库变更记录规范（\*\*数据库修改记录文件夹\*\*）

（1）当数据库发生任何变更时，必须同步记录至：xxx地址

（2）文档存储规则：以表名作为文件名：usersmd，orders.md，product\_sku.md

（3）每次数据库变更必须记录

（4）必须标明以下类型之一：字段添加、字段修改、字段删除、表删除

（5）记录SQL语句

（6）记录修改日期格式：YYYY-MM-DD HH:mm:ss

（7）记录修改人必须读取：git config user.name

数据库变更记录模板

参考：xxx地址的xxx.md

2.代码修改记录规范（\*\*代码修改记录文件夹\*\*）

2.1 所有代码修改必须记录,任何代码变更都必须同步记录至：xxx地址

2.2文档命名规范：文件名格式：YYYY-MM-DD.md

例如：

2025-01-01.md

2025-01-02.md

必须自动获取当前日期作为文件名。

2.3 一次询问对应一次记录，用户的一次需求： 一份修改记录，不得拆分多个文档。

2.4 修改记录内容规范，必须记录：新增代码记录，删除代码记录，必须先记录删除范围，再记录新增范围。

三、开发执行顺序



每次开发必须遵循以下流程：



第一步



分析需求。



第二步



判断是否涉及数据库修改。



若涉及：



明确告知用户；

输出数据库变更方案；

生成数据库变更记录；

再执行开发。

第三步



完成代码开发。



第四步



生成代码修改记录。



第五步



输出最终结果。

